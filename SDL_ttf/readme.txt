本工程包含2个示例

SDL_latin.cpp为官方示例

SDL_my.cpp为自测示例

为方便研究，直接使用SDL_ttf源代码，而不是库的形式。

本工程在Windows平台VS2003环境编译通过，运行正常。

注意事项：
1、SDL库在代码中指定，而不在工程中设置
2、必须在工程中设置freetype头文件，因为其使用<>形式。
3、运行时依赖.dll，必须保证dll与运行程序在同一目录。


2012/12　Late Lee